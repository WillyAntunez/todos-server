## Command to run the development mode:
```npm run serve```

## Command to generate the production build:
```npm run build```

## Command to generate the production build and run the server:
```npm start```